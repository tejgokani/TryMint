{
  "name": "@trymint/agent",
  "version": "1.0.0",
  "description": "TRYMINT Agent - Local CLI for command execution",
  "private": true,
  "main": "src/index.js",
  "bin": {
    "trymint": "./src/cli/index.js"
  },
  "scripts": {
    "dev": "Run agent in development mode",
    "build": "Build CLI for distribution",
    "test": "Run tests",
    "lint": "Lint source files",
    "link": "Link CLI globally for development"
  },
  "dependencies": {
    "Dependencies will include": "Commander, ws, node-pty, keytar, etc."
  },
  "devDependencies": {
    "Dev dependencies will include": "Testing, linting tools"
  }
}
