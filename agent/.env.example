# Agent connects to backend via WebSocket.
# Dev: leave unset (defaults to ws://localhost:3000)
# Prod: set to your Render backend URL (https or http; ws/wss derived automatically)
# TRYMINT_WS_URL=https://trymint-backend-xxxx.onrender.com
