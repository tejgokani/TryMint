{
  "name": "@trymint/frontend",
  "version": "1.0.0",
  "description": "TRYMINT Frontend - React Web Application",
  "private": true,
  "scripts": {
    "dev": "Start development server",
    "build": "Build for production",
    "test": "Run tests",
    "lint": "Lint source files",
    "preview": "Preview production build"
  },
  "dependencies": {
    "Dependencies will include": "React, Redux, WebSocket client, etc."
  },
  "devDependencies": {
    "Dev dependencies will include": "Testing, linting, build tools"
  }
}
